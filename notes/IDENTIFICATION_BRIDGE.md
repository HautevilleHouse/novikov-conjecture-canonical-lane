# Identification Bridge

Gate: `NOV_G6`
Constant: `eps_coh`

The determining class of higher-signature and assembly observables carries strict coherence. The gate closes exactly when the residual identification error is zero in strict mode.
