# EG1 Public Note

Gate: `NOV_G1`
Constant: `kappa_signature`

This note isolates the active response package. The projected response operator is ``E_tau = Pi_resp L_tau Pi_resp`.` The gate closes once the admissible tube carries a uniform positive floor on the projected response sector.
